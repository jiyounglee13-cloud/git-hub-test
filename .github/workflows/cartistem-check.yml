name: cartistem assets check

on:
  push:
    paths:
      - 'public/cartistem/**'
  pull_request:
    paths:
      - 'public/cartistem/**'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: 링크/이미지/JS 점검
        run: python3 public/cartistem/check_links.py
